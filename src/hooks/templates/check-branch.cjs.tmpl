// Auto-generated by autoevolve — {{DESCRIPTION}}
// Based on: {{BASED_ON}}
// Generated: {{GENERATED_AT}}

const { execSync } = require('child_process');
const toolInput = JSON.parse(process.env.TOOL_INPUT || '{}');
const command = toolInput.command || '';

// Only check for push/deploy commands
if (/\bgit\s+push\b/i.test(command) || /\bdeploy\b/i.test(command)) {
  try {
    const branch = execSync('git rev-parse --abbrev-ref HEAD', { encoding: 'utf8' }).trim();
    if (branch === 'main' || branch === 'master') {
      console.error('[autoevolve] 🛑 {{DESCRIPTION}}');
      console.error(`[autoevolve] Attempting to push/deploy from ${branch}. Use a feature branch.`);
      process.exit(2);
    }
  } catch {
    // not in a git repo, skip
  }
}
