// Auto-generated by autoevolve — {{DESCRIPTION}}
// Based on: {{BASED_ON}}
// Generated: {{GENERATED_AT}}

const toolInput = JSON.parse(process.env.TOOL_INPUT || '{}');
const command = toolInput.command || '';

const DESTRUCTIVE_PATTERNS = [
  /\bDROP\b/i, /\bDELETE\b/i, /\bTRUNCATE\b/i,
  /\brm\s+-rf\b/i, /\bgit\s+reset\s+--hard\b/i,
  /\bgit\s+push\s+--force\b/i,
];

const isDestructive = DESTRUCTIVE_PATTERNS.some(p => p.test(command));

if (isDestructive) {
  console.error('[autoevolve] 🛑 {{DESCRIPTION}}');
  console.error('[autoevolve] Potentially destructive operation detected. Confirm scope before proceeding.');
  process.exit(2); // non-zero blocks the tool use
}
