// Auto-generated by autoevolve — {{DESCRIPTION}}
// Based on: {{BASED_ON}}
// Generated: {{GENERATED_AT}}

const toolInput = JSON.parse(process.env.TOOL_INPUT || '{}');
const toolName = process.env.TOOL_NAME || '';

// Check if the command or file path references credentials/accounts
const SENSITIVE_PATTERNS = [
  /credential/i, /password/i, /secret/i, /api.?key/i,
  /\.env/i, /account/i, /profile/i, /token/i,
];

const input = JSON.stringify(toolInput);
const isSensitive = SENSITIVE_PATTERNS.some(p => p.test(input));

if (isSensitive) {
  console.error('[autoevolve] ⚠️  {{DESCRIPTION}}');
  console.error('[autoevolve] Detected sensitive data access. Verify the correct context is being used.');
}
